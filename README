A Drawing and Animation framework for teaching programmatic logic in Scala

To test the files run the following:

mkdir bin
scalac -d bin -sourcepath src src/*.scala
cd bin
scala -classpath bin examples/MyCanvas.scala
